name: flutter_base_app
description: Built for both iOS and Android platforms
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  # packages
  flutter_base_i18n:
    path: ../../packages/flutter_base_i18n
  flutter_base_lints:
    path: ../../packages/flutter_base_lints

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
